{
  "name": "electron_quick",
  "version": "1.0.0",
  "description": "electron_quick",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "electron . --inspect=9229 --remote-debugging-port=9223 electron_quick",
    "watch": "rollup -c ./rollup.config.js -w"
  },
  "author": "mm",
  "license": "ISC",
  "devDependencies": {
    "electron": "^31.0.1",
    "rollup": "^4.18.0"
  }
}
